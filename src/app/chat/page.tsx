import Chat from '@/components/chat/Chat';

const ChatPage = () => {
  return (
    <main className="relative flex flex-col flex-1 gap-5 justify-between">
      <Chat />
    </main>
  );
};

export default ChatPage;
